package com.example.hamparo.ui.theme

import androidx.compose.ui.graphics.Color

// Colores Principales (Modo Claro)
val HamparoPrimary = Color(0xFF006D77) // Turquesa oscuro elegante
val HamparoOnPrimary = Color(0xFFFFFFFF) // Texto blanco sobre turquesa
val HamparoPrimaryContainer = Color(0xFF83C5BE) // Turquesa suave

// Colores Secundarios (Para acciones importantes)
val HamparoSecondary = Color(0xFFE29578) // Terracota suave
val HamparoOnSecondary = Color(0xFFFFFFFF)

// Colores de Alerta/Error (Imprescindible para salud)
val HamparoError = Color(0xFFBA1A1A)
val HamparoOnError = Color(0xFFFFFFFF)

// Fondos (Cálidos para no cansar la vista)
val HamparoBackground = Color(0xFFFFFBFF)
val HamparoSurface = Color(0xFFFFFBFF)

// Colores Modo Oscuro (Por si acaso, aunque los mayores suelen preferir claro)
val HamparoPrimaryDark = Color(0xFF4DB6AC)
val HamparoSecondaryDark = Color(0xFFFFCCBC)